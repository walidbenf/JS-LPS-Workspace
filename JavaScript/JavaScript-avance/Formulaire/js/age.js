// Fonction qui affiche l'age
function/*function */afficheAge/*Nom de la fonction */(nom/*param1*/, annee/*param2*/){
var age;/* variable initiliase*/
age = 2019-annee;/* définir une valeur */
console.log(nom+ " a " + age + " an (s)");/*Un message dans la console */
}

afficheAge("amir",1997);/* On appelle la fonction*/

// Fonction qui calcule la moyenne
/*function calcMoy(nb1, nb2) {
  var moy;
  moy = (nb1 + nb2) / 2;
  return moy;
}
var m1, m2;
m1 = calcMoy(1000, 2000);
m2 = calcMoy(3455, 2);
alert(m1+ '/' + m2);
*/
var but = document.getElementById('butld');
but.addEventListener('click', resize);

function resize() {

}
